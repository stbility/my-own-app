# 个人工作生活专属 APP 产品需求文档 (PRD)

---

## 1. 产品目标和使用场景

### 1.1 产品目标
为个人用户打造一款完全属于自己的**单机本地桌面端“个人作战与生活控制中枢”**。聚合工作（自媒体、开发、咨询）与个人生活（健身、饮食、游戏、日常规划），解决分散使用多个通用待办软件、碎片化记录难以统一归纳、受制于网络与云端账号登录的痛点。

### 1.2 使用场景
- **使用人群**：仅供本人单独使用，无多用户、无协作、无外人访问需求。
- **使用环境**：电脑端常用浏览器（Chrome / Edge / Firefox / Safari 等桌面环境），开箱即用。
- **使用模式**：
  - 早晨打开：在首页一览今日核心要事与时间轴，快速记录早间闪念。
  - 工作期间：在今日计划中启用专注计时器，在自媒体/开发/咨询模块间针对具体任务执行看板流转、Bug记录与代码片段速查。
  - 生活健康：记录单次健身训练组数与负重、打卡日常饮水与饮食定性标签。
  - 晚间放松：查阅游戏库库藏与游玩心得，并可一键将全局数据备份导出到电脑本地硬盘。

---

## 2. 整体页面框架和导航结构

### 2.1 整体布局
采用现代桌面端控制台的**双栏式自适应结构**：
- **常驻左侧导航栏（Sidebar）**：
  - 支持展开与紧凑图标折叠（专注模式）。
  - 顶部显示应用专属标志与名称。
  - 导航区列出 9 大模块，每个项配备专属语义化图标与快捷数字徽标（如未完成任务数、在办项目数）。
  - 底部提供折叠开关与本地存储快速状态指示。
- **顶部辅助状态栏（Header）**：
  - 实时显示当前系统日期、星期。
  - 核心状态指示器：绿色脉冲点提示“本地数据已实时持久化”，打字即存，免去手动保存焦虑。
  - 快捷全局触发器：随时唤起“闪念备忘”浮层。
- **主视窗工作区（Main Content Canvas）**：
  - 宽屏无冗余留白卡片流，统一 8px/16px 网格韵律，杜绝无意义的卡片套卡片。

### 2.2 导航结构列表（9 个一级模块）
1. `⌂ 首页总览`（Dashboard）
2. `📅 今日计划`（Daily Plan）
3. `🎬 自媒体`（Content）
4. `💻 开发工作`（Dev Work）
5. `💼 咨询工作`（Consulting）
6. `🏋️ 健身计划`（Fitness）
7. `🥗 饮食计划`（Diet）
8. `🎮 游戏娱乐`（Gaming）
9. `⚙️ 数据与设置`（Data & Settings）

---

## 3. 首页的内容结构

首页遵循“**一览全局、聚焦今日、随手记录、快速下钻**”原则，具体包含四个核心功能区块：

1. **核心要事与状态横幅（Big 3 Header）**：
   - 根据时间自动显示亲和问候语与当前日期。
   - 直观呈现“今日三大核心要事”的完成进度环（如 `2/3 已完成`）。
2. **今日日程时间轴（Today Timeline）**：
   - 按早/中/晚或具体时刻列出今日已排期项，支持一键勾选完成。
   - 包含快速添加日程输入项。
3. **闪念速记便签池（Quick Capture Scratchpad）**：
   - 随手打字即时保存灵感想法。
   - 支持一键快捷“流转/转化”功能：可一键将某条便签发送至自媒体灵感库，或转为开发任务。
4. **各业务模块动态摘要矩阵（Module Glance Cards）**：
   - **自媒体动态**：显示创作管线中正在进行的选题文案或制作状态。
   - **开发工作动态**：显示当前正在进行的迭代项目及未结 P0/P1 Bug 数量。
   - **咨询工作动态**：显示近期待沟通的客户与跟进节点。
   - **健身打卡动态**：显示今日训练部位与完成状态。
   - **饮食饮水动态**：显示今日累计饮水量（如 `1500 / 2000 ml`）与三餐打卡情况。
   - **娱乐状态**：显示当前沉浸游玩中的游戏卡片。
   - 卡片均带有一键跳转对应模块的直达操作。

---

## 4. 每个模块的用途

| 模块名称 | 核心用途与解决的问题 |
| :--- | :--- |
| **1. 首页总览** | 汇总今日执行力与各领域最新情报，避免逐个页面翻查，作为启动工作的第一站。 |
| **2. 今日计划** | 专注当日执行，通过锁定每日最核心的 1~3 件要事与时段切块，配合轻量番茄钟抵抗分心。 |
| **3. 自媒体** | 支撑自媒体内容全生命周期管理（灵感选题 ➔ 脚本撰写 ➔ 剪辑制作 ➔ 发布复盘与多平台打标）。 |
| **4. 开发工作** | 专属开发者工作台，支撑项目卡片、需求与 Bug 排期跟踪、以及高频命令/代码 Snippets 片段库管理。 |
| **5. 咨询工作** | 管理客户咨询委托全流程，记录客户档案、阶段进度、单次会议纪要、工时与交付物追踪。 |
| **6. 健身计划** | 支撑日常力量/体能训练，提供周期分化排期、动作组数与负重递进记录（Log）以及体重打卡。 |
| **7. 饮食计划** | 建立轻量无压力的每日饮食打卡机制，跟踪一日三餐健康标签，并配有直观的每日饮水量打卡。 |
| **8. 游戏娱乐** | 追踪游戏待玩（Backlog）、在玩、通关与封盘状态，记录游玩时长与个人通关简评心得。 |
| **9. 数据与设置** | 承担系统的本地数据底座，支持全库一键 JSON 导出备份、导入还原、存储用量分析及数据重置。 |

---

## 5. 每个模块第一版最必要的数据和操作

### 5.1 今日计划（Daily Plan）
- **核心数据项**：
  - 今日 3 大要事（Title, Done, Priority）
  - 时间块日程（TimeSlot, Title, Status, Category）
  - 专注计时状态（Mode: 专注25m/休息5m, Running, RemainingSeconds）
- **核心操作**：
  - 增删改今日计划条目与设置 Big 3。
  - 点击完成打卡（带完成动效）。
  - 番茄钟开始 / 暂停 / 重置 / 切换时长模式。

### 5.2 自媒体（Content）
- **核心数据项**：
  - 内容卡片：标题、选题角度/大纲备注、所属阶段（选题池、大纲脚本、制作中、已发布）、目标平台（B站、小红书、公众号、抖音、知乎等）、发布日期、简要播放互动数据与复盘反思。
- **核心操作**：
  - 新建选题卡片与选择平台标签。
  - 看板流转：一键向前或向后推进阶段，或拖拽移动状态。
  - 编辑内容脚本大纲及撰写发布复盘笔记。

### 5.3 开发工作（Dev Work）
- **核心数据项**：
  - 项目实体：项目名称、描述、技术栈标签、本地目录路径备忘、代码仓库 Git 链接。
  - 待办/Bug 项：标题、严重度等级（P0紧急 / P1重要 / P2普通）、状态（待办 / 进行中 / 已解决）、所属项目。
  - 代码片段库（Snippets）：标题、语言/类别（如 Git / Docker / TS / Bash）、代码正文、说明。
- **核心操作**：
  - 切换当前激活项目，按项目筛选其待办与 Bug。
  - 添加/解决 Bug 或需求，修改等级与进展。
  - 新增代码片段，一键复制命令/代码内容至剪贴板（提供 Copied 成功提示）。

### 5.4 咨询工作（Consulting）
- **核心数据项**：
  - 客户档案：客户/公司名、所属行业、合作阶段（意向交流、已签约、服务中、已结项）、联系方式与备忘。
  - 咨询记录：所属客户、咨询日期时间、沟通要点/纪要、待交付任务清单、本次工时/费用记账。
- **核心操作**：
  - 新建/编辑客户档案并切换阶段。
  - 快速录入单次咨询会议纪要与跟进待办。
  - 标记交付物完成状态。

### 5.5 健身计划（Fitness）
- **核心数据项**：
  - 分化计划（如胸部/肱三头、背部/肱二头、腿部/核心、休息日）。
  - 今日训练日志：动作名称、分组列表（组号、重量 kg、次数 reps、是否完成）。
  - 打卡历史：日期、训练部位、持续时长、当天体重（kg）。
- **核心操作**：
  - 选择训练部位与动作，一键“+添加一组”，支持快速沿用上一组数据。
  - 勾选各组完成状态，完成今日训练打卡。
  - 记录与查看历史体重与训练频次。

### 5.6 饮食计划（Diet）
- **核心数据项**：
  - 今日饮水：目标总量（如 2000ml）、当前已饮总量（ml）、记录时间点。
  - 三餐与加餐条目：类型（早餐 / 午餐 / 晚餐 / 加餐）、食物内容描述、健康定性标签（高蛋白、低碳、高纤维、轻食低卡、高热量/放纵）。
- **核心操作**：
  - 快捷饮水打卡：点击 `+250ml` 按钮即时增加并更新进度水波/环形指示。
  - 快速录入餐食，轻点快捷勾选定性营养标签。
  - 切换查看过去几天的饮食打卡概括。

### 5.7 游戏娱乐（Gaming）
- **核心数据项**：
  - 游戏条目：游戏名、平台（Steam / PS5 / Switch / PC / Xbox）、状态（想玩/囤积、沉浸中、已通关、弃坑）、预估或已玩时长（h）、通关评级（1~5星）、心得评语。
- **核心操作**：
  - 新建游戏条目并打上平台与初始状态。
  - 更新游玩进度与累计时长。
  - 通关后一键打分并填写短评，转入封盘展示区。

### 5.8 数据与设置（Data & Settings）
- **核心数据项**：
  - 存储健康度：数据键值数量、LocalStorage 占用字节估算与百分比。
  - 备份元信息：最后备份时间、数据版本号。
- **核心操作**：
  - **导出全量备份**：将全部数据格式化为结构化 JSON 文件并触发浏览器下载（命名如 `my-life-hub-backup-YYYYMMDD.json`）。
  - **导入恢复数据**：支持本地文件选择或拖拽 `.json` 文件，提供覆盖恢复或增量合并预览，一键还原。
  - **载入预置示例数据**：一键生成各模块的真实场景体验数据，便于初次使用感知效果。
  - **重置与清空数据**：二次确认后清空本地缓存，恢复出厂洁净状态。

---

## 6. 首页、今日计划和各模块之间的关系

```
                     ┌───────────────────────┐
                     │     1. 首页总览       │
                     │ (实时汇总中心 & 聚合器) │
                     └───┬───────────────┬───┘
                         │ 产生/引用     │ 状态透视
                         ▼               ▼
           ┌─────────────────────┐   ┌─────────────────────────────┐
           │     2. 今日计划     │   │  3~8 垂直业务模块           │
           │ (今日三大要事/时间块)│   │  • 自媒体 (在制内容摘要)     │
           └──────────┬──────────┘   │  • 开发工作 (未解Bug/项目)   │
                      │ 关联推进      │  • 咨询工作 (近期日程)       │
                      └─────────────►│  • 健身/饮食 (今日打卡完成度)│
                                     │  • 游戏娱乐 (当前在玩卡片)   │
                                     └──────────────┬──────────────┘
                                                    │
                                                    ▼
                                     ┌─────────────────────────────┐
                                     │     9. 数据与设置           │
                                     │  (全模块统一持久化与备份)   │
                                     └─────────────────────────────┘
```

1. **聚合与透视关系**：
   - 首页不存储独立的实体数据（除临时便签外），它的各动态磁贴是其余各个专门模块的**轻量实时视图**（Read Model）。
2. **调度与执行关系**：
   - “今日计划”是每日行动总指挥，不仅可以列出日常日程，还可直接从开发、自媒体、咨询中提取今天必须解决的关键任务。
3. **灵感流转闭环**：
   - 首页的“闪念备忘”充当通用临时收集箱（Inbox），记录后可直接一键流转投递到“自媒体选题库”或“开发工作需求”，实现工作流闭环。
4. **底层存储归一**：
   - 所有模块通过统一的 LocalStorage 数据管理层（Storage Manager）进行读写，格式结构化、解耦且具备强类型校验，由“数据与设置”统一提供全局备份与灾备恢复。

---

## 7. 本地数据文件，备份和恢复要求

### 7.1 本地存储机制
- **存储载体**：优先基于浏览器标准 `window.localStorage`（轻量、同步、开箱即用、无并发事务锁）。
- **实时保存保障**：
  - 只要发生任何新增、编辑、删除、状态切换，数据模型立刻同步写入 LocalStorage。
  - 在浏览器刷新（F5）、彻底关闭标签页、退出浏览器甚至电脑重启后，再次打开页面数据 100% 完整保留。
- **存储键命名隔离**：所有存储键采用专属前缀命名（如 `my_hub_v1_store`），避免被同源其他缓存污染。

### 7.2 备份要求（导出）
- **文件格式**：标准的 `.json` 格式文本文件。
- **文件命名规范**：`my-hub-backup-YYYY-MM-DD_HHmmss.json`。
- **备份内容完整性**：备份包内必须包含当前全量 9 大模块所有数据项、数据架构版本号（schemaVersion）、导出时间戳。
- **纯本地导出**：通过前端内存 Blob 生成并触发下载，完全在用户浏览器本地完成，不经过任何外部网络服务器。

### 7.3 恢复要求（导入）
- **操作方式**：文件选择框（`<input type="file">`）与拖拽文件释放区双重支持。
- **校验与容错**：
  - 导入前进行 JSON 语法合法性校验与必要字段格式检测。
  - 若文件损坏或非本系统备份，给出清晰友好的中文错误拦截提示，不破坏当前已有数据。
  - 校验通过后瞬间原子写入本地存储并自动刷新视图，恢复率 100%。

---

## 8. 第一版（V1）必须完成的功能（In Scope）

- [x] **完整的 9 大模块专属视图**：
  - 首页总览（问候语、Big 3 环形进度、时间轴、闪念备忘板、各模块摘要卡片）
  - 今日计划（三大核心要事、分时段日程安排、内置番茄钟计时器）
  - 自媒体（选题灵感库、4阶段看板流转、多平台标签、发布复盘）
  - 开发工作（多项目分组、需求与Bug看板区分等级、高频代码片段速查与一键复制）
  - 咨询工作（客户合作全生命周期档案、咨询会话纪要、工时与交付记录）
  - 健身计划（分化训练排期、动作组数与负重记录器、体重与打卡历史）
  - 饮食计划（每日目标饮水量快捷点按记杯器、早中晚餐打卡、定性营养偏好标签）
  - 游戏娱乐（多平台游戏库卡片、在玩/通关/弃坑状态切换、时长记录与通关心得星级打分）
  - 数据与设置（一键 JSON 导出、一键 JSON 恢复导入、存储容量指示器、预置演示数据载入、清空重置）
- [x] **全模块纯本地持久化**：所有增删改动作自动保存至 LocalStorage。
- [x] **全局顶部指示与闪念速记**：绿色实时保存状态指示灯、无论在哪个页面均可唤出便签记录。
- [x] **桌面端高品质视觉与交互**：
  - 符合专业生产力软件标准的干净、高对比度排版，舒适的信息密度。
  - 状态切换微动效、完成打卡划线动效与操作即时反馈（Toast 提示）。

---

## 9. 第一版（V1）暂时不做的不功能（Out of Scope）

1. **不做用户账号体系**：不提供注册、登录、找回密码或个人资料修改。
2. **不做云端同步与远程数据库**：不依赖 Firebase、Supabase、PostgreSQL 等后端服务，无需任何网络连接。
3. **不做移动端与多端云同步**：专注桌面电脑宽屏浏览器，暂不针对小屏手机做深度适配。
4. **不做第三方开放平台 API 对接**：如不自动同步 Steam API 游戏库、不调用各内容平台数据统计接口，所有数据以个人自主记录为主。
5. **不做微观热量计算器**：不建立十万条食物卡路里/微量元素数据库，避免产生沉重的记录负担。
6. **不做深层嵌套的复杂多级报表**：避免过度设计，确保工具轻巧流畅。

---

## 10. 可以实际检查的可验收标准

| 验收维度 | 具体检查步骤与合格判定标准 |
| :--- | :--- |
| **AC-01: 本地数据持久化** | 1. 在今日计划或任意模块中添加 1 条新记录；<br>2. 按 `F5` 刷新页面或关闭浏览器重新打开；<br>**合格标准**：新添加的记录完好无损显示，无数据丢失。 |
| **AC-02: 首页聚合与直达** | 1. 在自媒体、开发、咨询、健身各模块录入数据；<br>2. 回到首页；<br>**合格标准**：首页各动态磁贴准确展示各模块最新在办事项，点击能直接跳转至对应模块。 |
| **AC-03: 便签一键流转** | 1. 在首页闪念便签写下一段点子；<br>2. 点击“转自媒体”或“转开发任务”；<br>**合格标准**：该点子顺利出现在对应模块的待办/选题池中。 |
| **AC-04: 各模块专属业务功能** | 1. **开发模块**：新增代码片段并点击“复制”，剪贴板成功获得代码；创建 Bug 并标记为 P0 等级；<br>2. **健身模块**：为动作添加 3 组负重与次数，勾选第 1 组完成；<br>3. **饮食模块**：点击 `+250ml`，饮水量立即增加且进度条正确更新；<br>4. **今日计划**：点击番茄钟“开始”，倒计时正常走字；<br>**合格标准**：各模块均呈现对应领域的专有字段与专属交互，无雷同表单。 |
| **AC-05: 备份导出与还原** | 1. 点击“数据与设置”中的【导出数据备份】；<br>2. 本地成功下载 `.json` 文件且内容为有效 JSON；<br>3. 在界面中删除几条记录，然后点击【导入备份】选择刚刚下载的文件；<br>**合格标准**：系统瞬时恢复到备份时的状态，被删数据完整复原。 |
| **AC-06: 零报错与无多余外部网络依赖** | 1. 打开浏览器控制台（F12 Console）；<br>2. 在离线状态下切换全部 9 个页面并执行操作；<br>**合格标准**：页面无阻塞性 JavaScript 异常，无 404 或由于缺少网络引发的白屏现象。 |
